TIARCA-023 onboarding preset previews

The six onboarding_preset_*.webp files are cropped from the real-device candidate-test screenshots supplied during TIARCA-006 testing. They are intentionally stored as separate nodpi resources so each preview can retain useful resolution instead of being extracted from the old compact six-frame sprite.
